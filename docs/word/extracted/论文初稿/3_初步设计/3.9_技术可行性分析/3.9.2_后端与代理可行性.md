# 3.9.2 后端与代理可行性

后端采用轻量级代理框架，能够集成主流大语言模型（如 DeepSeek-V3），已完成接口适配。代理的能力按场景静态配置（例如刷脸场景只允许导航和高亮，不具备填写敏感字段的能力），从设计层面保障了能力边界。会话状态与网络连接生命周期对齐，连接关闭时授权自动失效，无状态残留风险。

大语言模型的中文理解能力满足政务场景需求，API 调用成本低（估算月均不足10元），适合毕设规模。主要风险在于老年人口音的语音识别准确率，通过提示词调优和最多3次重试机制应对，并保留文字输入备选。
