# 3.7.3 触控目标规范

所有可交互元素的触控区均不小于48dp，参考WCAG移动端无障碍设计规范。底部Tab按钮的触控区为64×56dp，覆盖图标和标签整块。“助手”圆形按钮视觉直径为56dp，但触控区放大至88dp（含外圈）。面板内的麦克风大按钮触控区为88dp，作为核心操作进行最大化设计。授权弹窗中的“可以”和“不用了”两个按钮各为120×56dp，两按钮间距不小于16dp以防误触。关闭按钮（×）的触控区为44×44dp，位于右上角，满足移动端人机界面指南的最小尺寸要求。
